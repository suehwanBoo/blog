import { style } from "@vanilla-extract/css";

const wrapper = style({
  margin: "0 auto",
  width: 800,
  paddingBottom: 10,
  display: "flex",
  justifyContent: "flex-end",
});

export const postButtonStyles = { wrapper };
